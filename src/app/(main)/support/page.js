import SupportClient from "./components/SupportClient";

export default async function SupportPage() {
  return (
    <div className="support-page mt-[100px]">
      <SupportClient />
    </div>
  );
}
